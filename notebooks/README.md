# Notebooks

Use notebooks for exploration and visual inspection only. Reusable data conversion, training, evaluation, and figure logic belongs in `src/` and is called through `scripts/`.

Planned notebooks:

- `01_data_audit.ipynb` — consume the audit outputs; do not duplicate audit logic.
- `02_error_analysis.ipynb` — inspect frozen test summaries and selected cases.

Clear outputs before committing unless a compact output is intentionally part of the research narrative.

name: pancancer-nuclei
channels:
  - pytorch
  - nvidia
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - -e .

# This file defines the portable environment entry point. After the first
# successful install on the actual training machine, generate and commit an
# exact platform-specific lock file. Do not invent a lock before testing it.
